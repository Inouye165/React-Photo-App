# Root Environment Template for Local Development
# Copy this file to .env and fill in your actual values
# DO NOT COMMIT .env FILE TO GIT

# --- Frontend (Vite) ---
# Backend API base URL used by the React app
# - Local dev: Can be omitted (defaults to http://localhost:3001)
# - Production (Railway): Set to your public web URL, e.g.:
#   VITE_API_URL=https://web-production-XXXX.up.railway.app
VITE_API_URL=http://localhost:3001

# Supabase (Frontend - required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Enable E2E auth/session logic (only for Playwright/Cypress etc.)
# Set to 'true' ONLY in E2E test runs, never in normal dev/prod
VITE_E2E=false
# Optional: frontend host override
# VITE_PUBLIC_URL=http://localhost:5173

# Google Maps (Frontend)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
# Optional: Map ID (styling) appearing as 'mapId' in the Google Maps SDK; used for AdvancedMarker/styled maps
VITE_GOOGLE_MAPS_MAP_ID=your_google_map_id_here

# --- Note ---
# For server-side environment variables, see server/.env.example
# The server has its own .env file in the server/ directory
