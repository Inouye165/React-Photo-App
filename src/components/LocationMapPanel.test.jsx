// Deprecated shim: tests moved to LocationMapPanel.test.tsx
import { describe, it, expect } from 'vitest';

describe('LocationMapPanel.test.jsx deprecated', () => {
	it('delegates coverage to the TSX suite', () => {
		expect(true).toBe(true);
	});
});
