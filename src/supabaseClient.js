// Deprecated: kept as a compatibility shim for any tooling/imports still
// resolving to .js. The canonical implementation lives in supabaseClient.ts.
export { supabase } from './supabaseClient.ts';
