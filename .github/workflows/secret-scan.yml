name: Secret Scan & Server Tests

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['20']
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run secret-scan
        run: npm run secret-scan

  server-tests:
    runs-on: ubuntu-latest
    needs: secret-scan
    strategy:
      matrix:
        node-version: ['20']
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install root + server deps
        run: |
          npm ci
          cd server
          npm ci

      - name: Run server tests
        run: |
          cd server
          npm test --silent
name: Secret Scan & Server Tests

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
<<<<<<< HEAD
        node-version: [20]
=======
        node-version: ['20']
>>>>>>> e5a657e (chore: commit all ESLint/CI config and hooks (missed in previous push). .eslintcache is now gitignored.\n\nNote: Previous push did not include these because they were unstaged or untracked. Always check 'git status' before pushing to ensure all config and workflow files are included.)
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run secret-scan
        run: npm run secret-scan

  server-tests:
    runs-on: ubuntu-latest
    needs: secret-scan
    strategy:
      matrix:
<<<<<<< HEAD
        node-version: [20]
=======
        node-version: ['20']
>>>>>>> e5a657e (chore: commit all ESLint/CI config and hooks (missed in previous push). .eslintcache is now gitignored.\n\nNote: Previous push did not include these because they were unstaged or untracked. Always check 'git status' before pushing to ensure all config and workflow files are included.)
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install root + server deps
        run: |
          npm ci
          cd server
          npm ci

      - name: Run server tests
        run: |
          cd server
          npm test --silent
